# راهنمای مدرس: نسخه API-based

این نسخه تمرین را با API واقعی OpenRouter اجرا می‌کند، نه با مدل محلی Colab.

## فایل‌های لازم برای دانشجویان

- فایل notebook: telegram_policy_radar_4topics_openrouter_colab.ipynb
- فایل داده: telegram_policy_radar_4topics_fa.csv

## پیش از کلاس

1. از دانشجویان بخواهید در OpenRouter حساب بسازند و یک API key شخصی ایجاد کنند.
2. تأکید کنید کلید فقط در کادر مخفی Colab وارد می‌شود و نباید در notebook، چت، اسکرین‌شات یا فایل خروجی نوشته شود.
3. مدل پیش‌فرض openai/text-embedding-3-small است. مدل و قیمت را می‌توانید پیش از کلاس در فهرست مدل‌های embedding OpenRouter بررسی کنید.

## جریان فعالیت

1. دانشجو CSV را آپلود می‌کند.
2. چهار توضیح موضوعی را می‌بیند یا تغییر می‌دهد.
3. OpenRouter برای پست‌ها و توضیح‌ها embedding برمی‌گرداند.
4. cosine similarity نزدیک‌ترین موضوع را انتخاب می‌کند.
5. دانشجو توزیع، نقشه PCA، نمونه‌های نماینده، جست‌وجوی معنایی و موارد مبهم را تحلیل می‌کند.

این API workflow برای یک پیکره بزرگ‌تر نیز قابل گسترش است: batching، ثبت مصرف token، ذخیره بردارها، جست‌وجوی معنایی و بازبینی انسانی.

## یادآوری روش‌شناختی

نتیجه gold standard یا اندازه‌گیری دقت نیست. طبقه‌بندی به تعریف چهار موضوع حساس است و موارد با similarity پایین باید برای بازبینی پژوهشگر در اولویت باشند.
